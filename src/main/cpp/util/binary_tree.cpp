/* 
 * BinaryTree
 * A binary tree data structure
 */

// standard libary
#include <vector>

template <typename T>
class Composite {
private:
    std::vector<T> elements;

public:
    void hasNext(T element) {
    }
    void next(T element) {
    }
};
